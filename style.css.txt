/* ------------ ТЕМА / ПАЛИТРА ------------ */
:root {
  --ink: #121214;
  --bg: #fff;
  --soft: #f6f6f8;

  /* розовый градиент заголовков */
  --pink-1: #ff66c4;
  --pink-2: #ff8ad6;
  --pink-3: #ffb3e6;

  /* доп. акценты для ссылок/иконок */
  --violet: #8a7cff;
  --cyan: #6fe7ff;
}

* { box-sizing: border-box; }
html, body { margin: 0; padding: 0; }
body {
  background: var(--bg);
  color: var(--ink);
  font: 16px/1.6 "Inter", system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
}

/* ------------ ШАПКА ------------ */
.site-header {
  padding: 48px 20px 16px;
  text-align: center;
}
.site-header h1 {
  font-size: clamp(28px, 4vw, 48px);
  line-height: 1.1;
  margin: 0 0 8px;
  background: linear-gradient(90deg, var(--pink-1), var(--pink-2), var(--pink-3));
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}
.lead {
  margin: 0 auto 12px;
  max-width: 800px;
  opacity: .8;
}
.tagline a {
  display: inline-block;
  margin: 0 8px;
  padding-bottom: 2px;
  text-decoration: none;
  color: var(--ink);
  border-bottom: 2px solid var(--pink-2);
}
.tagline a:hover { color: var(--violet); border-color: var(--violet); }

/* ------------ КОНТЕЙНЕР ------------ */
.container {
  width: min(980px, 92vw);
  margin: 24px auto 80px;
  padding: 0 12px;
}

/* ------------ ЗАГОЛОВКИ ------------ */
h2 {
  font-size: clamp(22px, 3vw, 34px);
  margin: 32px 0 16px;
  background: linear-gradient(90deg, var(--pink-1), var(--pink-2));
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
}
.pink-sub {
  font-weight: 700;
  color: var(--ink);
  border-left: 6px solid var(--pink-2);
  padding-left: 10px;
  margin-top: 20px;
}

/* ------------ ТЕКСТ ------------ */
p, li { font-size: 1.02rem; }
code { background: #ffe8f6; padding: 0 4px; border-radius: 4px; }

/* ------------ ССЫЛКИ ------------ */
a { color: var(--ink); text-decoration: underline; text-decoration-style: wavy; text-decoration-color: var(--pink-2); }
a:hover { color: var(--violet); text-decoration-color: var(--violet); }

/* ------------ ФИГУРЫ / ОБТЕКАНИЕ ------------ */
figure { margin: 0 0 1rem; }
figcaption {
  font-size: .9rem;
  opacity: .75;
  margin-top: 6px;
}

/* плавающие блоки с обтеканием */
.float-left, .float-right {
  max-width: 46%;
  margin: .25rem 0 1rem;
}
.float-left  { float: left;  margin-right: 1rem; }
.float-right { float: right; margin-left: 1rem; }
.w40 { max-width: 40%; }

/* «форма» обтекания: круг или мягкий прямоугольник */
.shape-circle img, .shape-soft img { display: block; width: 100%; height: auto; }
.shape-circle { shape-outside: circle(50%); border-radius: 50%; overflow: hidden; }
.shape-soft   { shape-outside: inset(0 round 24px); border-radius: 16px; overflow: hidden; }

/* сброс обтекания после секции, чтобы ниже не «залипало» */
.clear { clear: both; }

/* ------------ Блоковые медиа ------------ */
.block-media { margin: 16px 0 20px; }
.video {
  width: 100%;
  max-height: 70vh;
  display: block;
  border-radius: 16px;
  background: #000;
}

/* респонсивное встраивание YouTube/Vimeo */
.embed {
  width: 100%;
  aspect-ratio: 16 / 9;
  background: #000;
  border-radius: 16px;
  overflow: hidden;
  margin: 12px 0 6px;
}
.embed iframe {
  width: 100%;
  height: 100%;
  border: 0;
}

/* ------------ СПИСКИ ССЫЛОК ------------ */
.links { padding-left: 1.2rem; }
.links li { margin: .25rem 0; }

/* ------------ ПРИМЕЧАНИЯ ------------ */
.note { font-size: .95rem; opacity: .75; }

/* ------------ ПОДВАЛ ------------ */
.site-footer {
  padding: 28px 20px 60px;
  text-align: center;
  background: var(--soft);
  color: #555;
}

/* ------------ МОБИЛЬНАЯ АДАПТАЦИЯ ------------ */
@media (max-width: 860px) {
  .float-left, .float-right, .w40 { float: none; max-width: 100%; margin: 0 0 1rem; }
  .shape-circle, .shape-soft { shape-outside: none; border-radius: 14px; }
}
